import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        secondary: {
            dark:         "#A663CC",
            main:         "#F26518",
            light:        "#FCDDF2",
            contrastText: "#FFFFFF"
        }
    }
});

